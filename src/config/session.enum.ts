export enum Sessions {
  AuthSession = 'aptos.social.auth.session',
}

export enum StorageKeys {
  AptosAccount = '@aptos.social/aptos-account',
  AptosEphemeral = '@aptos.social/aptos-ekp',
}

export enum Path {
  PROFILE_IMAGES = 'profile-images',
}
